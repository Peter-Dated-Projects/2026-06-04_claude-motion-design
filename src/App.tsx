import "./App.css";

// Minimal scaffold landing. The 3-panel studio UI (chat / code / preview)
// is built in a later ticket; this just confirms the Tauri window mounts React.
function App() {
  return (
    <main className="scaffold">
      <h1>Claude Motion</h1>
      <p>Scaffold ready. Studio UI coming next.</p>
    </main>
  );
}

export default App;
