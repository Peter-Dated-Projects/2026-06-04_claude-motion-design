# Research

Design proposals and investigations -- written-up explorations that inform a
future decision but are not themselves the committed decision (those graduate to
`decisions/`). Cite code paths; mark `status` as the proposal ages.

| Note | Summary | Status |
|---|---|---|
| [multi-file-architecture.md](multi-file-architecture.md) | Design proposal for multi-file ClaudeMotion projects: switch esbuild-wasm to bundle mode with a virtual-FS resolver plugin fed by a frontend file map; recommends building it AFTER preview stability (T-013) is confirmed; phased plan + honest risks. | current |
